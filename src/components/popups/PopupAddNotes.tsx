const PopupAddNotes = () => {
  return (
    <div>
      {/* {showForm && (
        <div className="mb-6 p-4 bg-white rounded-lg shadow-md">
          <input
            type="text"
            placeholder="Note Title"
            value={newNote.title}
            onChange={(e) => setNewNote({ ...newNote, title: e.target.value })}
            className="w-full mb-2 p-2 border border-gray-300 rounded"
          />
          <textarea
            placeholder="Note Content"
            value={newNote.content}
            onChange={(e) =>
              setNewNote({ ...newNote, content: e.target.value })
            }
            className="w-full h-24 p-2 border border-gray-300 rounded mb-2"
          ></textarea>
          <button
            onClick={handleAddNote}
            className="px-4 py-2 bg-pastel-blue text-gray-800 rounded-lg shadow-pastel hover:shadow-pastel-hover transition-all"
          >
            Save Note
          </button>
        </div>
      )} */}
    </div>
  );
};
export default PopupAddNotes;
